steps:
  # ステップ1: サービス存在チェックと適切な処理の実行
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "=== サービス存在チェック ==="
        
        # Cloud Runサービスとして存在するかチェック
        if gcloud run services describe hello-world --region=${_REGION} 2>/dev/null; then
          echo "✅ 既存のCloud Runサービスが見つかりました"
          DEPLOY_TYPE="update"
          
          echo "現在の設定:"
          gcloud run services describe hello-world --region=${_REGION} \
            --format="table(
              status.url,
              status.ingress,
              spec.template.spec.containers[0].resources.limits.memory,
              spec.template.spec.timeoutSeconds,
              spec.template.metadata.annotations['autoscaling.knative.dev/maxScale']
            )"
        else
          echo "📝 既存サービスが見つかりません"
          DEPLOY_TYPE="create"
        fi
        
        echo ""
        echo "デプロイタイプ: $DEPLOY_TYPE"
        echo "$DEPLOY_TYPE" > /workspace/deploy_type.txt
        
        echo ""
        echo "=== 置換変数の確認 ==="
        echo "リージョン: ${_REGION}"
        echo "メモリ: ${_MEMORY}"
        echo "タイムアウト: ${_TIMEOUT}"
        echo "最大インスタンス: ${_MAX_INSTANCES}"
        echo "最小インスタンス: ${_MIN_INSTANCES}"
        echo "環境: ${_ENVIRONMENT}"
        echo ""
    id: 'check-service'

  # ステップ2: 適切なデプロイ方法を実行
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        DEPLOY_TYPE=$(cat /workspace/deploy_type.txt)
        
        if [ "$DEPLOY_TYPE" = "update" ]; then
          echo "=== 既存サービスの更新 ==="
          
          # Cloud Runサービスとして更新
          gcloud run services update hello-world \
            --region=${_REGION} \
            --no-allow-unauthenticated \
            --ingress=all \
            --memory=${_MEMORY} \
            --timeout=${_TIMEOUT} \
            --max-instances=${_MAX_INSTANCES} \
            --min-instances=${_MIN_INSTANCES} \
            --set-env-vars=ENVIRONMENT=${_ENVIRONMENT},ACCESS_TYPE=authenticated,DEPLOY_TYPE=update \
            --verbosity=info
          
          echo "✅ 既存サービス更新完了"
          
        else
          echo "=== 新規関数の作成 ==="
          
          # Cloud Run Functionsとして新規作成
          gcloud functions deploy hello-world \
            --gen2 \
            --runtime=python311 \
            --region=${_REGION} \
            --source=./cloud_run_functions \
            --entry-point=hello_world \
            --trigger=http \
            --no-allow-unauthenticated \
            --ingress-settings=all \
            --memory=${_MEMORY} \
            --timeout=${_TIMEOUT} \
            --max-instances=${_MAX_INSTANCES} \
            --min-instances=${_MIN_INSTANCES} \
            --set-env-vars=ENVIRONMENT=${_ENVIRONMENT},ACCESS_TYPE=authenticated,DEPLOY_TYPE=create \
            --verbosity=info
          
          echo "✅ 新規関数作成完了"
        fi
        
        echo ""
        echo "=== デプロイ後の設定確認 ==="
        gcloud run services describe hello-world --region=${_REGION} \
          --format="table(
            status.url,
            status.ingress,
            spec.template.spec.containers[0].resources.limits.memory,
            spec.template.spec.timeoutSeconds,
            spec.template.metadata.annotations['autoscaling.knative.dev/maxScale'],
            spec.template.metadata.annotations['autoscaling.knative.dev/minScale']
          )"
    id: 'smart-deploy'
    waitFor: ['check-service']


substitutions:
  _REGION: 'asia-northeast1'
  _MEMORY: '256Mi'
  _TIMEOUT: '60s'
  _MAX_INSTANCES: '5'
  _MIN_INSTANCES: '0'
  _ENVIRONMENT: 'development'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

timeout: '1200s'