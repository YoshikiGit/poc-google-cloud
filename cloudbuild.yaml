steps:
  # ステップ1: サービス存在チェック
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "=== サービス存在チェック ==="
        
        # Cloud Runサービスとして存在するかチェック
        if gcloud run services describe hello-world --region=${_REGION} 2>/dev/null; then
          echo "✅ 既存のCloud Runサービスが見つかりました"
          echo "existing" > /workspace/service_status
          
          echo "現在の設定:"
          gcloud run services describe hello-world --region=${_REGION} \
            --format="table(status.url,status.ingress,spec.template.spec.containers[0].resources.limits.memory)"
        else
          echo "📝 既存サービスが見つかりません"
          echo "new" > /workspace/service_status
        fi
        
        SERVICE_STATUS=`cat /workspace/service_status`
        echo "サービス状態: ${SERVICE_STATUS}"
        
        echo ""
        echo "=== 設定値 ==="
        echo "リージョン: ${_REGION}"
        echo "メモリ: ${_MEMORY}"
        echo "タイムアウト: ${_TIMEOUT}"
        echo "最大インスタンス: ${_MAX_INSTANCES}"
        echo "環境: ${_ENVIRONMENT}"
    id: 'check-service'

  # ステップ2: 既存サービス更新
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        SERVICE_STATUS=`cat /workspace/service_status`
        
        if [ "${SERVICE_STATUS}" = "existing" ]; then
          echo "=== 既存サービスの更新 ==="
          
          gcloud run services update hello-world \
            --region=${_REGION} \
            --no-allow-unauthenticated \
            --ingress=all \
            --memory=${_MEMORY} \
            --timeout=${_TIMEOUT} \
            --max-instances=${_MAX_INSTANCES} \
            --min-instances=${_MIN_INSTANCES} \
            --set-env-vars=ENVIRONMENT=${_ENVIRONMENT},ACCESS_TYPE=authenticated \
            --verbosity=info
          
          echo "✅ 既存サービス更新完了"
        else
          echo "新規作成のため、このステップをスキップ"
        fi
    id: 'update-existing'
    waitFor: ['check-service']

  # ステップ3: 新規サービス作成
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        SERVICE_STATUS=`cat /workspace/service_status`
        
        if [ "${SERVICE_STATUS}" = "new" ]; then
          echo "=== 新規関数の作成 ==="
          
          gcloud functions deploy hello-world \
            --gen2 \
            --runtime=python311 \
            --region=${_REGION} \
            --source=./cloud_run_functions \
            --entry-point=hello_world \
            --trigger=http \
            --no-allow-unauthenticated \
            --ingress-settings=all \
            --memory=${_MEMORY} \
            --timeout=${_TIMEOUT} \
            --max-instances=${_MAX_INSTANCES} \
            --min-instances=${_MIN_INSTANCES} \
            --set-env-vars=ENVIRONMENT=${_ENVIRONMENT},ACCESS_TYPE=authenticated \
            --verbosity=info
          
          echo "✅ 新規関数作成完了"
        else
          echo "既存サービス更新のため、このステップをスキップ"
        fi
    id: 'create-new'
    waitFor: ['check-service']


substitutions:
  _REGION: 'asia-northeast1'
  _MEMORY: '256Mi'
  _TIMEOUT: '60s'
  _MAX_INSTANCES: '5'
  _MIN_INSTANCES: '0'
  _ENVIRONMENT: 'development'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

timeout: '1200s'